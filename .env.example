# ── Backend ──────────────────────────────────────────────────────────────────
PORT=3000
DB_PATH=./ci.db
LOG_LEVEL=info
WORK_DIR=/tmp/ci-runs

# ── Frontend (Vite proxy target) ─────────────────────────────────────────────
# Leave unset for local dev (defaults to http://localhost:3000)
# docker-compose sets this to http://api:3000 automatically
# API_URL=http://localhost:3000
